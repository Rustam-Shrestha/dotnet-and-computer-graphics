using System;
using System.Collections.Generic;
using System.Linq;
namespace LinkedListVsArraylist
{
    internal class LinkedListVsArraylist
    {
        //static void Main(string[] args)
        //{
        //    List<int> arrayList = new List<int>();
        //    LinkedList<int> linkedList = new LinkedList<int>();
        //    //populating the linked list and arraylist with the index numbers
        //    for (int i = 0; i < 1000000; i++) { 
        //        arrayList.Add(i);
        //        linkedList.AddLast(i);
        //    }
        //    // Calculating the timespan for accessing elements in ArrayList
        //    long startTimeAL = DateTime.Now.Ticks / TimeSpan.TicksPerMillisecond;
        //    int elementAL = arrayList.ElementAt(500000);
        //    long endTimeAL = DateTime.Now.Ticks / TimeSpan.TicksPerMillisecond;
        //    long durationAL = endTimeAL - startTimeAL;

        //    // Calculating the timespan for accessing elements in LinkedList
        //    long startTimeLL = DateTime.Now.Ticks / TimeSpan.TicksPerMillisecond;
        //    int elementLL = linkedList.ElementAt(500000);
        //    long endTimeLL = DateTime.Now.Ticks / TimeSpan.TicksPerMillisecond;
        //    long durationLL = endTimeLL - startTimeLL;

        //    Console.WriteLine("Time taken for ArrayList: "+durationAL +"ms");
        //    Console.WriteLine("Time taken for LinkedList: "+durationLL +"ms");



        //}
    }
}